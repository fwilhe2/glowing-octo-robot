# Downloaded source tarballs.
downloads

# The shared, cumulative staging tree every package installs into, and a glibc-only
# tree to compile against when it isn't rootfs/ itself.
rootfs
sysroot
glibc-package

# Built images, fetched CI artifacts and test console logs all land here.
output

# CI's scratch directory for downloaded artifacts.
artifacts

# Unpacked source trees, e.g. packages/bash/bash-5.3/
/packages/*/*-[0-9]*/
