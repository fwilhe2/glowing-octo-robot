*.tar.gz
*.tar.xz
rootfs
# a glibc-only staging tree to compile against, when it isn't rootfs/ itself
sysroot
glibc-package
output
boot-image/
boot-test.log
network-test.log
# unpacked source trees, e.g. bash/bash-5.3
/*/*-[0-9]*/
